// Variable assignment assigns unique type variable for all parameters and return value for top level functions. The type information of local variables are not propagated to other functions, so we skip them.

use crate::state::InferenceState;

pub fn assign_module_decl(state: &mut InferenceState) {
    
}